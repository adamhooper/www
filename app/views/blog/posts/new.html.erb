<h1>New post</h1>

<%= form_for([:blog, @post], :url => blog_posts_path) do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>

  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', blog_posts_path %>
