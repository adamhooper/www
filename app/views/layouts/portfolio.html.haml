- content_for(:body_class, 'portfolio')
- content_for(:title, 'Adam Hooper\'s Portfolio') unless content_for?(:title)
- content_for(:title_href_path, portfolio_pieces_path) unless content_for?(:title_href_path)
- content_for(:head) do
  = auto_discovery_link_tag(:rss, portfolio_pieces_path(:format => 'rss'))

= render(:template => 'layouts/application')
